{
  "name": "@satbank/database-types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'Build completed for database-types'",
    "dev": "echo 'Dev mode for database-types'",
    "lint": "echo 'Lint completed for database-types'",
    "type-check": "echo 'Type check completed for database-types'",
    "clean": "rm -rf dist"
  }
}