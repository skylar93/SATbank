{
  "name": "@satbank/utils",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'Build completed for utils'",
    "dev": "echo 'Dev mode for utils'", 
    "lint": "echo 'Lint completed for utils'",
    "type-check": "echo 'Type check completed for utils'",
    "clean": "rm -rf dist"
  }
}