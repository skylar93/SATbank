import { CreateExamClient } from '@/components/admin/CreateExamClient'

// Hybrid approach: Client-side auth with server-side data prefetching
export default function CreateExamPage() {
  return <CreateExamClient />
}