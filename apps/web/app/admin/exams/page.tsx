import { ExamsListClient } from '@/components/admin/ExamsListClient';

// Hybrid approach: Client-side auth with server-side data prefetching
export default function ExamManagementPage() {
  return <ExamsListClient />;
}